/**
 * @format
 */

import {AppRegistry} from 'react-native';
import ProjectGutenberg from './src';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => ProjectGutenberg);
